#include "holberton.h"

/**
* wildcmp - write a function that compares two strings
* @s1: substring one pointer
* @s2 :substring two pointer
*
* Description: the code returns 1 or 0
* Return: returns 1 or 0 depending
*/

int wildcmp(char *s1, char *s2)
{
	if (s1 > s2)
		return (0);
	return (1);
}
